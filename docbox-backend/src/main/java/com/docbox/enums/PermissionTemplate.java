package com.docbox.enums;

/**
 * Pre-defined permission templates for quick setup
 */
public enum PermissionTemplate {
    STANDARD,  // Balanced permissions for most family members
    LIMITED,   // Restricted access for younger children
    MINIMAL,   // Very limited access
    CUSTOM     // User-defined custom permissions
}